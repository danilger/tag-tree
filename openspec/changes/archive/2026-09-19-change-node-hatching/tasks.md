## 1. Canvas overlay styling

- [x] 1.1 Remove `CHANGE_BORDER` / green border and green box-shadow from `FileNode` when `changeOverlay` is true; do not apply solid `bgColor` that would hide the hatch
- [x] 1.2 Add `.file-node.change-overlay` pale gray cross-hatching background in `styles.css` (CSS comment: reserved for change-set display)

## 2. Docs

- [x] 2.1 Replace “green overlay” wording with reserved pale gray cross-hatching in README and DocsModal (and `changes/README.md` if it mentions green)
